<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="visual.camp.sample.app">


    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/book"
        android:label="가치봄"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AnalyzeActivity"></activity>

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />


        <activity android:name=".ourHabitChartActivity" />
        <activity android:name=".myHabitChartActivity" />
        <activity android:name=".tmpActivity" />
        <activity android:name=".balloonActivity" />
        <activity android:name=".Third_Fragment2_2" />
        <activity android:name=".Third_MainActivity" />
        <activity android:name=".Second_LoginActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity
            android:name=".First_SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.DemoActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>